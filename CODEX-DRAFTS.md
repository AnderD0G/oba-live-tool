# OBA Codex 评论草稿（本地定制版）

从官方 v1.6.1（commit 2a50be8690844704f0ab359c36742572a92a6320）起步，现已合并上游 v1.6.3 及截至 47c8b67 的更新。开发分支 `feat/codex-comment-drafts`。保留上游 MIT LICENSE。

## 抖音小店修复版 1.6.3-codex.10

- 同步上游修复：连接状态检测等待中控标记进入 DOM，避免首个匹配元素不可见时一直等待。
- 抖音小店连接阶段刷新页面，清理重复的中控元素，恢复按钮定位。小红书继续使用自身的打开新页面流程。
- 同步上游 tinyexec 依赖、模型列表及维护更新。保留定制应用身份、安装器、Codex、捕手和打印功能。
- 验证：35 项现有测试、3 项真实浏览器中的离线模拟测试（重复元素、登录跳转、小红书连接），以及 TypeScript 检查。未使用真实抖店账号发送消息或操作商品；上线连接情况需用户登录验证。

安装新版前退出旧版，使用相同安装位置更新。应用 ID 和用户数据路径不变，已有规则及打印队列按原存储保留；重启后仍需手动开启监听、捕获和自动打印。

## Windows 安装版 1.6.1-codex.9

从本 Fork 的 Releases 下载 Windows x64 安装程序，首次启动会检查浏览器、Codex 登录和 Windows 打印设备。复用兼容的已有 CLI；找不到时使用内置官方 Codex CLI 0.154.0。账号凭据、历史弹幕、打印队列、语音样本和模型不随安装包分发。使用 AI 仍需网络、登录和可用额度。

### 弹幕捕手与打印

- 自然语言规则通过常驻 Codex 对话澄清，用户确认后保存为受限的结构化策略；运行时在本地匹配，不逐条调用 AI。
- 数字、中文数字、全角数字、空格处理及长度限制可在确认前试算，保留编号前导零。
- 支持每条消息出单，或同批次同用户同号码只捕获一次；可另设每位观众每批次的出单上限。
- 可选择补筛本次打开已缓存的最多 5000 条弹幕；平台重复投递同一消息不会重复入单。
- 打印模块独立于捕获模块。队列与记录持久化；不明确的打印结果暂停，避免自动重打。
- 40 × 30 mm 标签使用大字号编号、昵称和用户 ID。Windows 提交成功不等于纸张已实际输出，请先预览和试打一张。
- 重启后监听、捕获及自动打印需自行开启。扣号记录是待核对单据，不代表平台已付款订单。
- 保留可选的本地语音服务接口，本安装版不部署语音服务或模型。

### 构建安装包

```text
pnpm install --frozen-lockfile
pnpm run test:codex
pnpm run build:installer
```

构建需联网下载固定版本的官方 Codex 原生组件及许可证；校验 SHA-512 后打入资源目录。Windows NSIS 模板使用临时短路径，避免 pnpm 深目录导致路径过长。产物位于 `release/<package.json 中的版本号>/`。`vendor-build/`、安装产物和用户数据均不进入 Git。执行 `pnpm run test:platform` 可运行离线连接测试，需要可用的 Edge / Chrome 或 Playwright Chromium。

发布验证：TypeScript 检查、35 项自动测试、内置 CLI 的无登录环境握手、独立空白应用配置首次启动和 NSIS 安装包完整性检查。没有通过验证脚本发送真实弹幕、调用模型或打印实体标签。安装包未做商业代码签名。

## 使用

1. 打开独立应用 `OBA-Codex.exe`，进入「自动回复」。
2. 「Codex 评论草稿」显示「常驻服务已就绪」及 PID 后，可先点「不直播，先试写」。测试不需要连接千帆或开启直播。
3. 展开「回复要求和模型」，输入主播语气及已确认的直播信息。模型留空使用 CLI 默认模型；可选填当前账号有权限使用的模型。
4. 真实评论通过原有千帆连接与「开始监听」进入列表。点某条文字评论旁的「Codex 拟回复」，再点「生成回复草稿」。
5. 草稿在该弹窗内显示，可编辑、复制或照着口播。**不会进入原有自动回复队列，也不会自动发送。** 原有「开始任务」是另一套自动回复功能，使用 Codex 草稿无需开启它。

在 1.6.1-codex.9 安装版中，到「安装与环境检查」点击「登录 ChatGPT」，完成浏览器登录后重新检测。旧目录版可在终端运行 `codex login`。账号认证和使用额度由 Codex CLI 管理。

手动点击生成，或开启下述自动回复后收到新评论时，会把评论正文和主播要求提交给 Codex；昵称、时间、其他评论、千帆登录信息不包含在提交内容里。回复要求和模型保存在本机；草稿不持久保存，关闭弹窗会丢弃手动草稿，关闭生成中的弹窗会取消该请求。每次调用独立、最多同时一条，120 秒超时。

## 自动生成并发送（1.6.1-codex.4）

连接小红书千帆并「开始监听」，在评论列表开启「Codex 自动回复 · 生成后发送」。不需要点击原有「开始任务」；启用 Codex 自动回复时会停止原有自动回复队列。开关使用开启时的回复要求、模型和黑名单，修改这些设置后需关闭再开启。

仅处理开启后收到的新文字评论，过滤自己、重复消息和黑名单用户。右侧显示排队、生成、发送和平台确认结果。串行处理，每条发送后至少间隔 5 秒，最多排队 10 条，等待超过一分钟的评论跳过。回复限制 100 字，超长或生成失败会暂停。

发送通过已登录千帆的评论输入框和按钮；已有手动输入时暂停，避免覆盖。只有 HTTP 成功、`success: true`、`code: 0`、`common_response.common_result: 1` 及返回相同正文才显示「已发送」；不明确的结果暂停，不自动重试，用户应查看直播间确认。平台确认不代表评论必然通过后续审核。

1.6.1-codex.5 修正了回执成功值：2026-09-08 真实发送返回 HTTP 200、`success: true`、`code: 0`、`common_result: 1`，用户同时确认观众手机收到。之前版本错误地期待 `common_result: 0`，导致已发送却显示未确认。保留仅含状态字段与正文长度的诊断日志，不记录回复正文、用户标识或登录凭据。同时修复窗口关闭过程中向已销毁网页发送状态通知的异常。

关闭开关会取消尚未发送的回复；已提交消息不能撤回。断开连接、停止监听、切换账号、刷新或重启后自动关闭。历史记录仅在当前运行中保留最近 50 条。

1.6.1-codex.3 在打开 OBA 后预启动一个 `codex app-server`，后续评论复用同一进程，无需手动唤醒。使用本地 stdio 管道，不开监听端口。每条评论仍使用独立的临时会话，完成后释放会话，防止不同评论串入上下文；正常取消只中断当前回复。进程异常退出后，下次请求自动重新启动；退出 OBA 时关闭它。

沿用 Electron 解析到的系统代理；已有的代理环境变量优先，不修改 Windows 全局设置。回复逐步显示，完成后可编辑复制，并显示「首字」「完成」耗时。生成中可以取消，网络重试有提示。启动和检测仅建立本地服务，不主动向模型发送预热评论。

## 开发与构建

Node.js + pnpm（本机验证 pnpm 11.19.0）。

```text
pnpm install --frozen-lockfile
pnpm exec tsc --noEmit
pnpm run test:codex
pnpm run test:codex-live
pnpm run build:local
```

`test:codex-live` 会真实调用已登录 Codex三次，消耗账号额度；内容为合成测试评论。记录首字/完整回复耗时和每次 PID 是否相同。此独立脚本不经过 Electron，若环境需要代理，请给运行该脚本的进程设置 HTTP_PROXY/HTTPS_PROXY。

若 Electron 二进制自动下载失败，可从 Electron 官方发布页下载与 `node_modules/electron/package.json` 版本一致的 Windows x64 zip，按该包 `checksums.json` 校验后解压到 `node_modules/electron/dist`，并写入 `path.txt` 为 `electron.exe`。本机使用 Electron 36.9.5，已通过 SHA256 校验。

本机离线复用 Electron 目录打包命令：

```text
pnpm exec vite build
pnpm exec electron-builder --win --dir --publish never --config.electronDist=node_modules/electron/dist
```

依赖锁文件已纳入版本管理；构建时将 Electron node-gyp 固定为同项目发布到 npm 的版本，避免间接 Git 安装。`motion/react` 替代了上游未声明依赖的 `framer-motion` 导入。

## 设计与验证

- Electron 主进程固定 IPC 接口；原生 spawn 参数数组、shell=false、评论通过 UTF-8 stdin。
- 常驻进程使用独立临时目录；各评论 read-only、ephemeral。复用用户登录和默认模型，显式覆盖本客户端的工具/项目文档/开发者指令配置；禁用 shell、浏览器、MCP 服务器、插件、Hooks 等，不修改用户配置。进程退出时清理临时目录。
- `CodexAppServer` 维护一次初始化、请求 ID 映射、错误与进程退出处理；`PersistentCodexDraftService` 处理独立评论会话、进度和取消。上一版 exec 服务保留供测试对比，应用入口使用常驻服务。
- 原始 stderr 不回传界面，避免将内部诊断/认证信息带入评论草稿。
- 按请求 ID 和窗口归属取消；窗口销毁时终止所属请求；UI 关闭弹窗也会取消。
- 独立应用名/数据目录；关闭上游自动更新，避免定制功能被官方二进制覆盖；默认关闭上游固定的调试端口。
- 定制安装包未签名，通过本 Fork 的 Releases 分发；原安装版仍可独立使用。

已验证：TypeScript 检查、15 项自动测试（包含同进程复用、隔离、取消、进程恢复、新评论筛选、自发消息过滤、发送结果不明确时暂停、关闭后禁止发送、真实成功回执结构及窗口关闭保护）。使用 Codex CLI 0.153.4 的真实合成评论测试：进程启动约 0.35 秒，三条回复首字 5.4/5.3/6.2 秒、完成 6.8/5.8/6.5 秒，均为同一个 PID。这不是延迟保证，也不能把旧版约 11 秒全部归因于进程启动。真实自动回复已由观众手机确认收到；1.6.1-codex.5 新评论复测返回同一成功结构，右侧即时显示「已发送／平台已确认发送」，自动回复开关保持开启。

官方 CLI 调用说明：https://learn.chatgpt.com/docs/non-interactive-mode

常驻协议：https://learn.chatgpt.com/docs/app-server 。该接口目前标为实验性；CLI 升级后需要重新验证握手与事件兼容性。
